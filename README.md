# You just reached Rebakure👋

## 🚀 Let's connect:

- kevinrebakure@gmail.com
- LinkedIn [Kevin Rebakure](https://www.linkedin.com/in/kevin-rebakure-91063a301/)

```ts
export class IBuildGoodSoftware {
  private frontEnd: string[];
  private design: string[];
  private theseComesInHandyOnFrontend: string[];
  private backEnd: string[];
  private getTheData: string[];
  private cantSurviveWithoutThese: string[];
  private bulletProofSoftware: string[];
  private myFav: string[];
  private languages: string[];
  private hobbies: string[];
  private nerdy: string[];
  private ai: string[];
  private productivity: string[];

  constructor() {
    this.frontEnd = ['NextJS', 'React', 'React Native'];
    this.design = ['Figma', 'TailwindCSS'];
    this.theseComesInHandyOnFrontend = [
      'TailwindCSS',
      'React Router',
      'Redux | Zustand',
    ];
    this.backEnd = ['NestJs'];
    this.getTheData = ['PostgresSQL', 'MongoDB', 'Prisma | TypeORM'];
    this.cantSurviveWithoutThese = ['Git', 'Docker'];
    this.bulletProofSoftware = ['Jest'];
    this.myFav = ['Appwrite'];
    this.languages = ['TypeScript', 'JavaScript', 'Python'];
    this.hobbies = ['Guitar'];
    this.nerdy = ['I read docs for curiosity'];
    this.ai = ['Claude', 'ChatGPT', 'WindSurf'];
    this.productivity = ['Notion', 'Spotify'];
  }

  workWithAGoodDeveloper() {
    return [
      ...this.frontEnd,
      ...this.design,
      ...this.theseComesInHandyOnFrontend,
      ...this.backEnd,
      ...this.getTheData,
      ...this.cantSurviveWithoutThese,
      ...this.bulletProofSoftware,
      ...this.myFav,
      ...this.nerdy,
      ...this.languages,
    ];
  }

  haveFun() {
    return [...this.hobbies, ...this.productivity];
  }

  IHearALotOfNoiseWithAI() {
    return [...this.ai];
  }
}
```
